# THIS FILE HAS BEEN GENERATED BY THE BUILD CONFIGURATION SYSTEM - DO NOT EDIT

set(MINECPP_DISCOCLI_DIR ${CMAKE_CURRENT_SOURCE_DIR})

add_executable(minecpp_tool_discocli)

# Public include headers
target_include_directories(minecpp_tool_discocli PUBLIC ${CMAKE_CURRENT_SOURCE_DIR}/include)

# Source subdirectory
add_subdirectory("src")

target_link_libraries(minecpp_tool_discocli
        LINK_PRIVATE
        minecpp_discovery_api
        grpc++
        grpc
        gpr
        fmt
)
